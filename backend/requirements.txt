# Veyra backend dependencies (installed into C:\VEYRA\.venv on Python 3.14).
# The Security Kernel itself is pure stdlib; these power the M2+ API/agent layer.
fastapi==0.136.3
uvicorn[standard]==0.49.0
httpx==0.28.1
edge-tts
python-multipart
