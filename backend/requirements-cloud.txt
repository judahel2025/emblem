# Cloud brain dependencies — the always-on server build.
# Excludes the heavy LOCAL-only stack (torch / faster-whisper / coqui-tts / piper),
# which only matters on your PC. Everything here is needed by the cloud tools.
fastapi==0.136.3
uvicorn[standard]==0.49.0
httpx==0.28.1
python-multipart
edge-tts
python-docx
reportlab
python-pptx
openpyxl
pypdf
matplotlib
ddgs
psycopg[binary]
websockets
google-genai
composio
